services:

  test_app:
    build:
      context: .
      dockerfile: Dockerfile.test
    container_name: test_app
    env_file:
      - .env.test
    depends_on:
      - test_db

  test_db:
    image: postgres:17
    container_name: test_db
